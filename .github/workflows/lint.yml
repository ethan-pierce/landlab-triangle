name: Lint

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Keep `version` in sync with the ruff pin in pyproject.toml
      # ([tool.pixi.feature.dev.dependencies]) so CI and local pixi runs agree.
      # `src: ""` stops the action appending the workspace path to `args`;
      # without it the effective target is `src test .`, which also lints
      # README code blocks and diverges from the `pixi run` tasks.
      - name: Ruff lint
        uses: astral-sh/ruff-action@v3
        with:
          version: 0.16.3
          src: ""
          args: check src test

      - name: Ruff format check
        uses: astral-sh/ruff-action@v3
        with:
          version: 0.16.3
          src: ""
          args: format --check src test
